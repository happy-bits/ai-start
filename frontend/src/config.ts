/**
 * Application configuration
 * 
 * Set developerTools to true during development/testing
 * to enable quick login buttons and other debugging features.
 */
export const config = {
  /**
   * Enable developer tools in the UI.
   * When true, shows quick login buttons on the login page
   * and other helpful testing features.
   */
  developerTools: true,
} as const;

