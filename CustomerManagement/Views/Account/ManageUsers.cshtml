@model IEnumerable<CustomerManagement.Models.ApplicationUser>

@{
    ViewData["Title"] = "Hantera användare";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Hantera användare</h2>
    <a asp-action="CreateUser" class="btn btn-primary">Skapa ny användare</a>
</div>

<div class="card">
    <div class="card-body">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Namn</th>
                            <th>E-post</th>
                            <th>Telefon</th>
                            <th>Registrerad</th>
                            <th>Senast uppdaterad</th>
                            <th>Åtgärder</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@user.FirstName @user.LastName</td>
                                <td>@user.Email</td>
                                <td>@(user.PhoneNumber ?? "-")</td>
                                <td>@user.CreatedAt.ToString("yyyy-MM-dd HH:mm")</td>
                                <td>@user.UpdatedAt.ToString("yyyy-MM-dd HH:mm")</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <button type="button" class="btn btn-sm btn-outline-primary" 
                                                onclick="viewUser('@user.Id')">Visa</button>
                                        <button type="button" class="btn btn-sm btn-outline-warning" 
                                                onclick="editUser('@user.Id')">Redigera</button>
                                        <button type="button" class="btn btn-sm btn-outline-danger" 
                                                onclick="deleteUser('@user.Id')">Ta bort</button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-4">
                <p class="text-muted">Inga användare hittades.</p>
                <a asp-action="CreateUser" class="btn btn-primary">Skapa första användaren</a>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        function viewUser(userId) {
            // Implementera visa användare
            alert('Visa användare: ' + userId);
        }
        
        function editUser(userId) {
            window.location.href = '@Url.Action("EditUser", "Account")' + '?id=' + userId;
        }
        
        function deleteUser(userId) {
            if (confirm('Är du säker på att du vill ta bort denna användare?')) {
                // Implementera ta bort användare
                alert('Ta bort användare: ' + userId);
            }
        }
    </script>
}
